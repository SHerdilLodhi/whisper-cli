# claude_speak

## Run

```bash
# GUI app
cd "/home/sherdil-lodhi/Documents/PR/CLI Speak Command Tool"
.venv/bin/claude_speak_gui

# GUI app (background, terminal stays free)
.venv/bin/claude_speak_gui &

# CLI only (no GUI)
.venv/bin/claude_speak
```
